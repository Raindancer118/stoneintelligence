<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>de.raindancer118</groupId>
  <artifactId>stoneintelligence</artifactId>
  <version>0.4.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>StoneIntelligence</name>
  <description>
    Enterprise-Grade Kollaborations- und Wissensmanagement-Plattform für Obsidian:
    Live-Sync, Note-Level-Policies inkl. E2EE, KI-gestützte Wissens-Ingestion und
    Agent-Zugriff über MCP.
  </description>

  <modules>
    <module>domain-core</module>
    <module>platform-api</module>
    <module>intelligence-worker</module>
    <module>mcp-adapter</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <java.version>25</java.version>
    <maven.compiler.release>25</maven.compiler.release>
    <spring-boot.version>4.0.0</spring-boot.version>

    <junit-jupiter.version>6.0.0</junit-jupiter.version>
    <assertj.version>3.27.6</assertj.version>
    <testcontainers.version>1.21.3</testcontainers.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>de.raindancer118</groupId>
        <artifactId>domain-core</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit-jupiter.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>${assertj.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <release>${maven.compiler.release}</release>
            <parameters>true</parameters>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <executions>
            <execution>
              <goals>
                <goal>integration-test</goal>
                <goal>verify</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <!--
          Ohne spring-boot-starter-parent als Eltern-POM (dieses Projekt importiert
          spring-boot-dependencies nur als BOM) bindet Boot die repackage-Goal NICHT automatisch
          an die package-Phase - ohne diese Executions erzeugt "mvn package" ein Jar ohne
          Main-Class-Manifest ("no main manifest attribute"), das sich nicht per `java -jar`
          starten laesst. Betrifft alle drei Spring-Boot-Module (platform-api,
          intelligence-worker, mcp-adapter) gleichermassen, deshalb hier zentral in der
          Root-pluginManagement statt dreifach dupliziert.

          Bewusst mit `classifier=exec`: ohne Classifier ERSETZT repackage das duenne Haupt-
          Artefakt durch das fette Boot-Jar (BOOT-INF/classes/... statt flacher Paketstruktur) -
          platform-api hat IT-Tests im selben Modul (maven-failsafe-plugin, Phase
          integration-test, laeuft NACH package), deren Classpath-Aufloesung dann u.U. auf dieses
          fette Jar statt auf target/classes greift und Klassen wie AuthorizationRepository nicht
          mehr findet (NoClassDefFoundError). Mit Classifier bleibt das duenne Jar das
          Haupt-Artefakt, das fette, ausfuehrbare Jar entsteht zusaetzlich als
          `<artifact>-exec.jar` (s. Dockerfiles).
        -->
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <executions>
            <execution>
              <goals>
                <goal>repackage</goal>
              </goals>
              <configuration>
                <classifier>exec</classifier>
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <distributionManagement>
    <repository>
      <id>packages-releases</id>
      <url>https://packages.tstieh.de/releases</url>
    </repository>
    <snapshotRepository>
      <id>packages-snapshots</id>
      <url>https://packages.tstieh.de/snapshots</url>
    </snapshotRepository>
  </distributionManagement>

</project>
